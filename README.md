# Factory Config Editor

A simple GUI tool to edit `~/.factory/config.json` configuration files.

## Installation

```bash
pip install .
```

## Usage

Run the following command in your terminal:

```bash
factory-editor
```

This will open the graphical editor allowing you to add, remove, and modify model configurations.
